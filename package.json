{
  "private": true,
  "packageManager": "pnpm@9.0.6",
  "scripts": {
    "dev": "pnpm --filter pixabots-app dev",
    "build": "pnpm --filter @pixabots/core build && pnpm --filter pixabots-app build",
    "generate-grids": "node generate-grids.mjs"
  },
  "devDependencies": {
    "next": "16.2.3"
  }
}
