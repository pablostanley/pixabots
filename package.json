{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter pixabots dev",
    "build": "pnpm --filter @pixabots/core build && pnpm --filter pixabots build",
    "generate-grids": "node generate-grids.mjs"
  }
}
